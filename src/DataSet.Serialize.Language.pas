unit DataSet.Serialize.Language;

{$IF DEFINED(FPC)}
  {$MODE DELPHI}{$H+}
  {$codePage utf8}
{$ENDIF}

interface

type
  /// <summary>
  ///   Languages handled by helper Validate (JSON).
  /// </summary>
  TLanguageType = (ptBR, enUS);

implementation

end.
